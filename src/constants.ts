// The true, intrinsic export dimensions of every slide (Instagram 4:5 portrait).
// Slide components are designed at THIS pixel scale; the preview only *visually*
// shrinks them via a transform on an ancestor wrapper (never on the slide node).
export const SLIDE_W = 1080;
export const SLIDE_H = 1350;
